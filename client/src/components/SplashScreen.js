import Typography from '@mui/material/Typography'

export default function SplashScreen() {
    return (
        <div id="splash-screen">
            <Typography variant="h2">Playlister</Typography>
        </div>
    )
}